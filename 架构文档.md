# model
## InstanceRegistration
值对象，实例分布式注册模型
### 包
com.guo.im.server.core.registry.instance
### 属性
* instanceId
* host
* port
* metadata<String,Object>
## IMChannelRegistration
值对象，IMChannel分布式注册模型
### 包
com.guo.im.server.core.registry.channel
### 属性
* connectId
* instanceId
* metadata<String,Object>
## BindkeyRegistration
值对象，绑定key模型
## 包
com.guo.im.server.core.registry.bindkey
## 属性
* bindKey
* deviceId
* connectId
## InboundMessage
类，定义了IM核心之间传递的消息对象，由客户端发往服务端
## 包
com.guo.im.server.common.message
## 属性
* String messageId
* int cmd
* Long timestamp
* Object payload
## OutboundMessage
类，与客户端交互的消息模型，服务端出站消息模型
### 包
com.guo.im.server.common.mesaage
### 属性
* String messageId
* int cmd
* Long timestamp
* Object payload
## BizCommand
类，与业务层交互的消息对象，由IM核心发往业务层
### 包
com.guo.im.server.core.biz.model
### 属性
* String bindKey
* String deviceId
* Object payload
## DispatchMessage
类，作为业务系统往channel推送消息模型
## 包
com.guo.im.server.core.message
## 属性【待定】
* bindKey
* deviceId 非必填为空则取默认取最早注册的设备
* pushType 推送类型为空则默认只发给一个目标
* Object payload
## ClusterMessage
类，定义了IM核心之间请求的消息对象，由IM核心发往目标IM核心
### 包
com.guo.im.server.core.internal.model
### 属性【待定】
* String messageId
* String sourceInstanceId
* String targetInstanceId
* Object payload
## ClusterMessageErrorContext
值对象，定义了集群消息错误报告上下文
## 包
com.guo.im.server.core.exception.model
## 属性
* ClusterMessage clusterMessage
* MessageFailStageEnum messageFailStage
* String reason



# InstanceRegistry
接口，可以通过实现该接口来注册服务，可以拓展内存、redis、zookeeper、nacos来实现
## 包
com.guo.im.server.core.registry.instance
## 方法
* void register(String instanceId,String host,int port,Map<String,Object> metadata)
* boolean deregister(String instanceId)
* List<InstanceRegistration> getInstances(Collection<String> instanceIds)
* void watchInstances(InstanceChangeListener listener)
接口，定义了服务实例的注册、查询、监听、删除
# InstanceChangeListener
接口，服务实例监听器，watchInstances方法，监听服务实例变化
## 包
com.guo.im.server.core.registry.instance
## 方法
* void onInstancesChanged(String type,InstanceRegistration instance)
# IMChannelRegistry
接口，定义了IMChannel的注册、查询、监听、删除
## 包
com.guo.im.server.core.registry.channel
## 方法
* void register(String instanceId,String connectId,Map<String,Object> metadata)
* boolean deregister(String connectId)
* List<IMChannelRegistration> getIMChannels(Collection<String> connectIds)
* void watchIMChannels(IMChannelChangeListener listener)
# IMChannelChangeListener
接口，IMChannel监听器，watchIMChannels方法，监听IMChannel变化
## 包
com.guo.im.server.core.registry.channel
## 方法
* void onIMChannelsChanged(String type,List<IMChannelRegistration> imChannels)
# BindkeyRegistry
接口，定义了BindKey的注册、查询、监听、删除
## 包
com.guo.im.server.core.registry.bindkey
## 方法
* void register(String bindKey,String deviceId,String connectId,Map<String,Object> metadata)
* boolean deregister(String bindKey,String deviceId)
* List<BindkeyRegistration> getBindkeys(Collection<String> bindKeys)
* void watchBindkeys(BindkeyChangeListener listener)
# BindkeyChangeListener
接口，BindKey监听器，watchBindkeys方法，监听BindKey变化
## 包
com.guo.im.server.core.registry.bindkey
## 方法
* void onBindkeysChanged(type,List<BindkeyRegistration> bindkeys)
# Cache
接口，提供缓存级别的 put方法、get方法、delete方法、expire方法、increment方法
## 包
com.guo.im.server.core.cache
## 方法
* void put(String key,Object value)
* E get(String key,Class<E> clazz)
* delete(String key)
* expire(String key,long timeout,TimeUnit timeUnit)
* increment(String key,long delta)
# ConnectRouter
接口，连接路由器，route方法，负责返回根据connectid所在的实例信息
## 包
com.guo.im.server.core.route
## 方法
* IMChannelRegistration route(String connectId)
# MessageDispatcher
接口，消息分发器，dispatch方法，将消息分发到对应的渠道
## 包
com.guo.im.server.core.dispatch
## 方法
* void dispatch(DispatchMessage dispatchMessage)
# Outlet
接口，定义消息输出的接口，send方法，将消息输出到对应的渠道
## 包
com.guo.im.server.core.dispatch
## 方法
* void send(String instanceId,String key,ClusterMessage clusterMessage)
# Inlet
接口，定义消息输入任务的接口，将消息从渠道内取出
## 包
com.guo.im.server.core.dispatch
## 方法
* List<ClusterMessage> receive(String instanceId,String key, int batchSize)
# MessagePump
抽象类，存在一个定时方法，收集了所有的MessageProcessor，根据MessageProcessor对应监听的名字到Inlet去获取消息，取出消息后交给对应的MessageProcessor进行处理
## 包
com.guo.im.server.core.dispatch
## 属性
* Collection<MessageProcessor> messageProcessors
## 方法
* MessagePump(Collection<MessageProcessor> messageProcessors) 构造方法
* void run() 模板方法
* void start()
* stop()
# MessageProcessor
接口，定时消息处理器
## 包
com.guo.im.server.core.dispatch
## 方法
* String topic() 拉取主体
* int batchSize() 每次拉取数量
* int period() 运行间隔
* void onMessage(ClusterMessage message)
# IMEndpoint
接口，定义了IM端点。需要拓展的服务就是实现该接口。【在与客户端连接完成、断开连接的时候必须调用IMChannelLifecycle】
## 包
com.guo.im.server.core.endpoint
## 方法
* void start()
* void stop()
* boolean isReady()
# IMConnect
值对象，记录着连接、端点、实例等信息
## 包
com.guo.im.server.core.model
## 属性
* connectId
* pointId
* instanceId
# IMChannel
## 包
com.guo.im.server.core.channel
接口，定义了我的IM的标准渠道规范，因为端点的实现是不限定技术的所以需要定义IMChannel接口作为交互的规范，没有read方法是因为要支持异步且read的逻辑应该由业务处理器承接
## 方法
* String getConnectId()
* boolean isActive()
* write(OutboundMessage outboundMessage)
* close()
# IMEventPublisher
接口，定义了事件发布器的规范
## 包
com.guo.im.server.core.publish
## 方法
* void publish(IMEvent imEvent)
# IMEventListener
接口，定义了服务监听器的规范
## 包
com.guo.im.server.core.listener
## 方法
* boolean supports(Object object)
* void onEvent()
# Lifecycle
接口，定义了生命周期的规范，在实例启动时往往会伴随一些额外的工作需要跟随生命周期一起完成，比如消息流、注册等
## 包
com.guo.im.server.core.lifecycle
## 方法
* void start()
* void stop()
# CompositeIMEventPublisher
类，实现了IMEventPublisher接口，核心作用是将所有IMEventPublisher实现对象折叠到此处进行调用
## 包
com.guo.im.server.core.publish
## 属性
* List<IMEventPublisher> imEventPublishers
## 方法
* CompositeIMEventPublisher(List<IMEventPublisher> imEventPublishers) 构造方法
* void publish(IMEvent imEvent)
# CoreIMEventPublisher
类，实现了IMEventPublisher接口，器作为核心发布器，其一定会被添加到CompositeIMEventPublisher中，因为这是框架内部所依赖的发布器，所以必须存在
## 包
com.guo.im.server.core.publish
## 方法
* void publish(IMEvent imEvent)
# MessageFailEventListener
类，实现了 IMEventListener 接口，当消息处理失败时，会调用此发布器
## 包
com.guo.im.server.core.listener
## 方法
* boolean supports(Object object) 只支持MessageFailEvent
* void onEvent(IMEvent imEvent) 处理失败事件将失败消息调用AbstractMessageErrorReporter的子类的reportError方法
# GIMHandler注解
注解，定义了IMProcessor的处理规范
## 包
com.guo.im.server.core.annotation
## 属性
* cmd
# IMProcessor
接口，作为外部业务开发器的规范，需要搭配GIMHandler注解进行使用标识其处理业务
## 包
com.guo.im.server.core.processor
## 方法
* void process(BizCommand bizCommand)
# ServerManager
作为实例的承载点，启动的时候会让IMEndpoint进行启动
## 包
com.guo.im.server.core
## 属性
* Collection<IMEndpoint> IMEndpoint
* CompositeIMEventPublisher publisher
## 方法
* ServerManager(Collection<IMEndpoint> IMEndpoint, CompositeIMEventPublisher publisher) 构造方法
* start() 启动方法
* stop() 停止方法
* isReady() 判断实例是否就绪
* publish(IMEvent imEvent)
# IMChannelLifecycle
抽象类，在IMEndpoint需要创建IMChannel的时候会调用
## 包
com.guo.im.server.core.channel
## 方法
* void channelCreated(IMChannel imChannel) 固定逻辑创建默认的bindkey并调用onChannelCreated方法
* abstract void onChannelCreated(IMChannel imChannel)
* void channelClosed(String connectId) 删除对应的bindkey并调用onChannelClosed方法
* abstract void onChannelClosed(String connectId)
* abstract void bindKey(String oldBindKey,String newBindKey)
# IMChannelHolder
类，储存了IMChannel的实例，以及ConnectEvent实例所对应的事件的发布
## 方法
* IMChannel getChannel()
* void putChannel()
* void removeChannel()
# BizFilter
接口，定义了在进入对应的AbstractIMProcessor之前的过滤器，类似JavaWeb的过滤器
## 包
com.guo.im.server.core.filter
## 方法
* void doFilter(BizCommand bizCommand, BizFilterChain filterChain)
* default boolean supports(int cmd)
# BizFilterChain
接口，定义了过滤器链的规范
## 包
com.guo.im.server.core.filter
## 方法
* void doFilter()
# MessageExceptionHandler
接口，定义了在消息传递过程中失败的处理逻辑
## 包
com.guo.im.server.core.exception
## 方法
* void handleException(DispatchMessage dispatchMessage, String reason) 入参后期可改造成异常
# ClusterMessageErrorReporter
接口，定义了在消息传递过程中失败的处理逻辑，在消息流内消息传递过程中失败时会调用该抽象类的入口方法
## 包
com.guo.im.server.core.exception
## 方法
* void reportMessageError(ClusterMessageErrorContext context)
# DefaultClusterMessageErrorReporter
类，实现了ClusterMessageErrorReporter，在消息传递过程中失败时会调用该抽象类的入口方法
## 包
com.guo.im.server.core.exception
## 方法
* void reportMessageError(ClusterMessageErrorContext context) 默认将消息重新投递会源节点，如果源节点下线则调用本实例实现的MessageExceptionHandler接口的实现类




# IMEvent
类，定义了本项目的事件基类
## 包名
com.guo.im.server.core.event
## 方法
* getEventId方法
* getTimestamp方法
# InstanceEvent
类，定义了实例事件，继承了IMEvent
## 属性
* instanceId
# InstanceStartEvent
类，实现了 InstanceEvent 接口，记录了实例启动事件
# InstanceReadyEvent
类，实现了 InstanceEvent 接口，记录了实例就绪事件
# InstanceStopEvent
类，实现了 InstanceEvent 接口，记录了实例停止事件
# EndpointEvent
类，定义了端点事件，继承了IMEvent
## 方法
* getInstanceId方法
* getEndpointId方法
# EndpointStartEvent
类，实现了 EndpointEvent 接口，记录了端点启动事件
# EndpointReadyEvent
类，实现了 EndpointEvent 接口，记录了端点就绪事件
# EndpointStopEvent
类，实现了 EndpointEvent 接口，记录了端点停止事件
# ConnectEvent
密封接口，定义了连接事件，继承了IMEvent
## 方法
* getConnectId方法
* getInstanceId方法
* getEndpointId方法
# ConnectReadyEvent
类，实现了 ConnectEvent 接口，记录了连接就绪事件
# ConnectEndEvent
类，实现了 ConnectEvent 接口，记录了连接断开事件
# BindKeyEvent
类，定义了BindKey事件，继承了IMEvent
## 方法
* getBindKey方法
* getDeviceId方法
* getConnectId方法
# BindKeyCreateEvent
类，实现了 BindKeyEvent 接口，记录了BindKey创建事件
# BindKeyUnbindEvent
类，实现了 BindKeyEvent 接口，记录了BindKey解绑事件
# ClusterMessageAckEvent
类，定义了消息确认事件，继承了IMEvent
## 方法
* getMessageId方法
* getSourceInstanceId方法
* getTargetInstanceId方法
# ClusterMessageSuccessAckEvent
类，实现了 MessageAckEvent 接口，记录了消息确认成功事件
# ClusterMessageFailAckEvent
类，实现了 MessageAckEvent 接口，记录了消息确认失败事件
## 包
com.guo.im.server.core.event
# 方法
* getMessage方法
* getMessageFailStage方法 确认消息是在什么阶段失败的
* String reason方法

# 欠缺架构
## MessageDispatcher缺少响应
## 在某些地方应该保留持久化的口子



# 二开功能点
## 注册功能增加本地缓存功能







~~## Filter
类，过滤条件
### 包
com.guo.im.server.core.data.spec
### 属性
* field
* operator
* value
## Operator
枚举，定义了操作符
### 包
com.guo.im.server.core.data.spec
### 枚举
EQ, NEQ, GT, GTE, LT, LTE, IN, LIKE, BETWEEN, EXISTS, MATCH
### Page
类，定义了分页信息
### 属性
* pageNumber
* pageSize
## Projection
类，定义了返回什么字段
### 包
com.guo.im.server.core.data.spec
### 属性
* fields
## Query
类，定义了查询条件
### 包
com.guo.im.server.core.data.spec
### 属性
* filters
* sorts
* page
* projection
## Sort
类，定义了排序条件
### 包
com.guo.im.server.core.data.spec
### 属性
* field
* direction
## Direction
Sort的内部枚举类，定义了排序规则
## Entity
抽象类，定义了持久化的共享属性
### 包
com.guo.im.server.core.data
### 属性
* id
* createTime
* updateTime
## EntityName
注解，定义了表名称
### 包
com.guo.im.server.core.data.annotation
### 属性
* value
## EntityFieldName
注解，定义了字段名称
### 包
com.guo.im.server.core.data.annotation
### 属性
* value

-------------

# DataStore
接口，提供了持久化的 保存、查询、删除
## 包
com.guo.im.server.core.data
## 方法
* void save(Class<E> clazz, E entity)
* E findById(Class<E> clazz, Long id)
* List<E> query(Class<E> clazz, Query<E> query)
* int count(Class<E> clazz, Query<E> query)
* void delete(Class<E> clazz, Collection<Long> ids)
~~