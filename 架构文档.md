# model
## ServiceInstance
类，分布式注册模型
### 包
com.guo.server.core.registry.model
### 属性
* serviceName
* serviceId
* host
* port
* mataData<String,Object>

# RegistryClient
接口，可以通过实现该接口来注册服务，可以拓展内存、redis、zookeeper、nacos来实现
## 包
com.guo.server.core.registry
## 方法
* register(String serviceName,String serviceId,String host,int port,Map<String,Object> mataData)
* deregister(String serviceName,String serviceId)
* List<ServiceInstance>getInstances(String serviceName)、(String serviceName,String serviceId)
* watchInstances(String serviceName,ServiceInstancesChangeListener listener)
# ServiceInstancesChangeListener
接口，服务实例监听器，watchInstances方法，监听服务实例变化
## 包
com.guo.server.core.registry
## 方法
* onInstancesChanged(String serviceName, List<ServiceInstance> instances)
# DataStore
接口，提供了持久化的 保存、查询、删除
## 包
com.guo.server.core.data
## 方法
* put(String key,Object value)
* get(String key,Class cls)
* delete(String key)
# Cache
接口，提供缓存级别的 put方法、get方法、delete方法、expire方法、increment方法
## 包
com.guo.server.core.cache
## 方法
* put(String key,Object value)
* get(String key,Class cls)
* delete(String key)
* expire(String key,long timeout,TimeUnit timeUnit)
* increment(String key,long delta)
# ConnectRouter
接口，连接路由器，route方法，负责返回根据connectid所在的实例信息
# MessageDispatcher
接口，消息分发器，dispatch方法，将消息分发到对应的渠道
# Outlet
接口，定义消息输出的接口，send方法，将消息输出到对应的渠道
# Inlet
接口，定义消息输入任务的接口，receive方法，将消息从渠道内取出
# MessagePump
类，存在一个定时方法，收集了所有的MessageProcessor，根据MessageProcessor对应监听的名字到Inlet去获取消息，取出消息后交给对应的MessageProcessor进行处理
# MessageProcessor
接口，定义了topic方法，作为消息拉取的key，定义了onMessage方法作为消息处理的方法
# IMEndpoint
接口，定义了IM端点，有 start方法、stop方法、isReady等方法。需要拓展的服务就是实现该接口。【在与客户端连接完成、断开连接的时候必须调用IMChannelLifecycle】
# IMConnect
值对象，记录着 connectId、pointId、instanceId
# IMChannel
接口，定义了我的IM的标准渠道规范，因为端点的实现是不限定技术的所以需要定义IMChannel接口作为交互的规范，没有read方法是因为要支持异步且read的逻辑应该由业务处理器承接
## 方法
* getConnectId方法
* isActive方法
* write方法
* close方法。
# IMEvent
接口，定义了事件的最小规范
## 方法
* getId方法
* timestamp方法
# ServerEvent
接口，继承了IMEvent，定义了服务启动、停止、异常、ready等事件
## 方法
* getInstanceId方法
* getEndpointId方法
# ConnectionEvent
接口，继承了IMEvent，定义了连接建立、断开、异常、ready等事件
## 方法
* getConnectId方法
# IMEventPublisher
接口，定义了事件发布器的规范
## 方法
* publish方法
# IMEventListener
接口，定义了服务监听器的规范
## 方法
* supports方法
* onEvent方法
# Lifecycle
接口，定义了生命周期的规范，在实例启动时往往会伴随一些额外的工作需要跟随生命周期一起完成，比如消息流、注册等
## 方法
* start方法
* stop方法
# CompositeIMEventPublisher
类，实现了IMEventPublisher接口，核心作用是将所有IMEventPublisher实现对象折叠到此处进行调用
# CoreIMEventPublisher
类，实现了IMEventListener接口，器作为核心发布器，其一定会被添加到CompositeIMEventPublisher中，因为这是框架内部所依赖的发布器，所以必须存在
# GIMHandler注解
注解，定义了IMProcessor的处理规范
## 属性
* cmd
# AbstractIMProcessor
抽象类，作为外部开发器的基类，需要搭配GIMHandler注解进行使用标识其处理业务
## 方法
* process方法
* transForm方法
# ServerManager
作为实例的承载点，启动的时候会让IMEndpoint进行启动
# IMChannelLifecycle
接口，在IMEndpoint需要创建IMChannel的时候会调用
## 方法
* channelCreated方法
* channelClosed方法
# IMChannelContext
类，实现了储存了IMChannelLifecycle，储存了IMChannel的实例，以及IMChannel实例所对应的IMEventPublisher实例
## 方法
* getChannel方法
* setChannel方法
* channelCreated方法 保存IMChannel实例、发布channel事件
* channelClosed方法 删除IMChannel实例、发布channel事件
## 方法
* start方法
* stop方法
* isReady方法
* publish方法