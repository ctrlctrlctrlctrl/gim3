# model
## InstanceRegistry
值对象，实例分布式注册模型
### 包
com.guo.server.core.registry.model
### 属性
* instanceId
* host
* port
* mataData<String,Object>
## IMChannelRegistry
值对象，IMChannel分布式注册模型
### 包
com.guo.server.core.registry.model
### 属性
* connectId
* instanceId
* mataData<String,Object>
## Filter
类，过滤条件
### 包
com.guo.server.core.data.spec
### 属性
* field
* operator
* value
## Operator
枚举，定义了操作符
### 包
com.guo.server.core.data.spec
### 枚举
EQ, NEQ, GT, GTE, LT, LTE, IN, LIKE, BETWEEN, EXISTS, MATCH
### Page
类，定义了分页信息
### 属性
* pageNumber
* pageSize
## Projection
类，定义了返回什么字段
### 包
com.guo.server.core.data.spec
### 属性
* fields
## Query
类，定义了查询条件
### 包
com.guo.server.core.data.spec
### 属性
* filters
* sorts
* page
* projection
## Sort
类，定义了排序条件
### 包
com.guo.server.core.data.spec
### 属性
* field
* direction
## Direction
Sort的内部枚举类，定义了排序规则
## Entity
抽象类，定义了持久化的共享属性
### 包
com.guo.server.core.data
### 属性
* id
* createTime
* updateTime
## EntytiName
注解，定义了表名称
### 包
com.guo.server.core.data.annotation
### 属性
* value
## EntytiFieldName
注解，定义了字段名称
### 包
com.guo.server.core.data.annotation
### 属性
* value
## Message
待定，信息的媒介

# InstancesRegistry
接口，可以通过实现该接口来注册服务，可以拓展内存、redis、zookeeper、nacos来实现
## 包
com.guo.server.core.registry
## 方法
* void register(String instanceId,String host,int port,Map<String,Object> mataData)
* boolean deregister(String instanceId)
* List<InstanceRegistry> getInstances(Collection<String> instanceIds)
* void watchInstances(InstancesChangeListener listener)
接口，定义了服务实例的注册、查询、监听、删除
# InstancesChangeListener
接口，服务实例监听器，watchInstances方法，监听服务实例变化
## 包
com.guo.server.core.registry
## 方法
* void onInstancesChanged(List<ServiceInstance> instances)
# IMChannelRegistry
接口，定义了IMChannel的注册、查询、监听、删除
## 包
com.guo.server.core.registry
## 方法
* void register(String instanceId,String connectId,Map<String,Object> mataData)
* boolean deregister(String connectId)
* List<IMChannelRegistry> getIMChannels(Collection<String> connectIds)
* void watchIMChannels(IMChannelsChangeListener listener)
# IMChannelsChangeListener
接口，IMChannel监听器，watchIMChannels方法，监听IMChannel变化
## 包
com.guo.server.core.registry
## 方法
* void onIMChannelsChanged(List<IMChannelRegistry> imChannels)
# DataStore
接口，提供了持久化的 保存、查询、删除
## 包
com.guo.server.core.data
## 方法
* void save(Class<E> clazz, E entity)
* E findById(Class<E> clazz, Long id)
* List<E> query(Class<E> clazz, Query<E> query)
* int count(Class<E> clazz, Query<E> query)
* void delete(Class<E> clazz, Collection<Long> ids)
# Cache
接口，提供缓存级别的 put方法、get方法、delete方法、expire方法、increment方法
## 包
com.guo.server.core.cache
## 方法
* void put(String key,Object value)
* E get(String key,Class<E> clazz)
* delete(String key)
* expire(String key,long timeout,TimeUnit timeUnit)
* increment(String key,long delta)
# ConnectRouter
接口，连接路由器，route方法，负责返回根据connectid所在的实例信息
## 包
com.guo.server.core.route
## 方法
* IMChannelRegistry route(String connectId)
# MessageDispatcher
接口，消息分发器，dispatch方法，将消息分发到对应的渠道
## 包
com.guo.server.core.dispatch
## 方法
* void dispatch(Message? message)
# Outlet
接口，定义消息输出的接口，send方法，将消息输出到对应的渠道
## 包
com.guo.server.core.dispatch
## 方法
* void send(String instanceId,String key,Message? message)
# Inlet
接口，定义消息输入任务的接口，将消息从渠道内取出
## 包
com.guo.server.core.dispatch
## 方法
* Message? receive(String instanceId,String key)
# MessagePump
抽象类，存在一个定时方法，收集了所有的MessageProcessor，根据MessageProcessor对应监听的名字到Inlet去获取消息，取出消息后交给对应的MessageProcessor进行处理
## 包
com.guo.server.core.dispatch
## 属性
* Collection<MessageProcessor> messageProcessors
## 方法
* MessagePump(Collection<MessageProcessor> messageProcessors) 构造方法
* void run() 模板方法
* void start()
* stop()
# MessageProcessor
接口，定时消息处理器
## 包
com.guo.server.core.dispatch
## 方法
* String topic() 拉取主体
* int batchSize() 每次拉取数量
* int period() 运行间隔
* void onMessage(Message message)
# IMEndpoint
接口，定义了IM端点。需要拓展的服务就是实现该接口。【在与客户端连接完成、断开连接的时候必须调用IMChannelLifecycle】
## 包
com.guo.server.core.endpoint
## 方法
* void start()
* void stop()
* boolean isReady()
# IMConnect
值对象，记录着连接、端点、实例等信息
## 包
com.guo.server.core.?
## 属性
* connectId
* pointId
* instanceId
# IMChannel
## 包
com.guo.server.core.?
接口，定义了我的IM的标准渠道规范，因为端点的实现是不限定技术的所以需要定义IMChannel接口作为交互的规范，没有read方法是因为要支持异步且read的逻辑应该由业务处理器承接
## 方法
* String getConnectId()
* boolean isActive()
* write(Buffer buffer)
* close()
# IMEventPublisher
接口，定义了事件发布器的规范
## 方法
* void publish(IMEvent imEvent)
# IMEventListener
接口，定义了服务监听器的规范
## 方法
* boolean supports()
* void onEvent()
# Lifecycle
接口，定义了生命周期的规范，在实例启动时往往会伴随一些额外的工作需要跟随生命周期一起完成，比如消息流、注册等
## 方法
* void start()
* void stop()
# CompositeIMEventPublisher
类，实现了IMEventPublisher接口，核心作用是将所有IMEventPublisher实现对象折叠到此处进行调用
## 包
com.guo.server.core.publish
## 属性
* List<IMEventPublisher> imEventPublishers
## 方法
* CompositeIMEventPublisher(List<IMEventPublisher> imEventPublishers) 构造方法
* void publish(IMEvent imEvent)
# CoreIMEventPublisher
类，实现了IMEventListener接口，器作为核心发布器，其一定会被添加到CompositeIMEventPublisher中，因为这是框架内部所依赖的发布器，所以必须存在
## 包
com.guo.server.core.publish
## 方法
* void publish(IMEvent imEvent)
# GIMHandler注解
注解，定义了IMProcessor的处理规范
## 包
com.guo.server.core.annotation
## 属性
* cmd
# AbstractIMProcessor
抽象类，作为外部业务开发器的基类，需要搭配GIMHandler注解进行使用标识其处理业务
## 包
com.guo.server.core.processor
## 方法
* void process(E e)
* E transForm(Object o)
# ServerManager
作为实例的承载点，启动的时候会让IMEndpoint进行启动
## 包
com.guo.server.core
## 属性
* Collection<IMEndPoint> IMEndPoint
* CompositeIMEventPublisher publisher
## 方法
* ServerManager(Collection<IMEndPoint> IMEndPoint, CompositeIMEventPublisher publisher) 构造方法
* start() 启动方法
* stop() 停止方法
* isReady() 判断实例是否就绪
* publish(IMEvent imEvent)
# IMChannelLifecycle
接口，在IMEndpoint需要创建IMChannel的时候会调用
## 包
com.guo.server.core.?
## 方法
* void channelCreated(IMChannel imChannel)
* void channelClosed(String connectId)
# IMChannelContext
类，实现了储存了IMChannelLifecycle，储存了IMChannel的实例，以及ConnectEvent实例所对应的事件的发布
## 方法
* IMChannel getChannel()
## 方法
* start方法
* stop方法
* isReady方法
* publish方法

# IMEvent
密封接口，定义了本项目的事件基类
## 包名
com.guo.server.core.event
## 方法
* getId方法
* timestamp方法
# InstanceEvent
密封接口，定义了实例事件，继承了IMEvent
## 方法
* getInstanceId方法
# InstanceStartEvent
值对象，实现了 InstanceEvent 接口，记录了实例启动事件
# InstanceReadyEvent
值对象，实现了 InstanceEvent 接口，记录了实例就绪事件
# InstanceStopEvent
值对象，实现了 InstanceEvent 接口，记录了实例停止事件
# EndpointEvent
密封接口，定义了端点事件，继承了IMEvent
## 方法
* getInstanceId方法
* getEndpointId方法
# EndpointStartEvent
值对象，实现了 EndpointEvent 接口，记录了端点启动事件
# EndpointReadyEvent
值对象，实现了 EndpointEvent 接口，记录了端点就绪事件
# EndpointStopEvent
值对象，实现了 EndpointEvent 接口，记录了端点停止事件
# ConnectEvent
密封接口，定义了连接事件，继承了IMEvent
## 方法
* getConnectId方法
* getInstanceId方法
* getEndpointId方法
# ConnectReadyEvent
值对象，实现了 ConnectEvent 接口，记录了连接就绪事件
# ConnectEndEvent
值对象，实现了 ConnectEvent 接口，记录了连接断开事件
